name: Periodic health check for website

on:
  schedule:
      - cron: '30 12,13,14,15 * * *'

jobs:
  check_website_health:
    runs-on: ubuntu-20.04
    steps:
    - name: Check the deployed service URL
      uses: jtalk/url-health-check-action@v3
      with:
        url: https://cicd-blogs.fly.dev/health
        max-attempts: 3
        retry-delay: 5s